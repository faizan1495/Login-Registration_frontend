# How to Run the Project

To run the project, follow these steps:

1. Download the project to your local machine.

2. Open your terminal.

3. Navigate to the project directory.

4. Run the following command to install project dependencies: npm install 

5. Once the dependencies are installed, start the project by running: npm start

The project should now be running locally. Open your web browser and navigate to the appropriate URL to view the application.
